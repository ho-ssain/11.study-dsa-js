/* TypedArray was created so that we could work with arrays with a single datatype.
Its syntax is let myArray = new TypedArray(length), where TypedArray needs to be
replaced with one TypedArray class, */

let uint8Array = new Uint8Array(10);
console.log("🚀 ~ uint8Array:", uint8Array);
